shared {
  let Sum = 0;
}

program A(my_id: int) {
  loop {
    Sum += 1;
    Sum -= 1;
  }
}

check {
  always if Sum == 2 { eventually Sum == 1 };
}

main {
  let n = 2;
  let a:list(proc(A));
  for i in 0..n {
    let p = run A(i);
  }
}
